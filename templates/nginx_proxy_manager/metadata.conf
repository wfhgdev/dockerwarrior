# ==============================================================================
# DockerWarrior - Manifiesto de Aplicación (DW-AppSpec v1.2)
# Paquete Piloto: Nginx Proxy Manager
# ==============================================================================

# --- Identificación del Módulo ---
APP_ID="nginx_proxy_manager"
APP_VERSION="2.11.1"
APP_CATEGORY="Network / Proxy"

# --- Dependencias del Ciclo de Vida ---
APP_REQUIRES_DB="true"
APP_DB_ENGINE="mariadb"
APP_GLOBAL_NETWORK_DEPENDENCY="dw_proxy_network"

# ==============================================================================
# EXTENSIÓN V1.2: METADATOS AUTÓNOMOS PARA EL DEPLOYMENT REPORT
# ==============================================================================
# Estos valores alimentan la API de report.sh de forma agnóstica al núcleo.

# Nombre descriptivo que se mostrará en el inventario técnico
APP_REPRESENTATION_NAME="Nginx Proxy Manager"

# Protocolo base de acceso perimetral (http / https)
APP_PROTOCOL="http"

# Puerto de publicación por defecto para la interfaz de administración
APP_DEFAULT_PORT="81"

# Estado de ciclo de vida esperado tras el aprovisionamiento del Core
APP_EXPECTED_STATUS="Preparado en el volumen de infraestructura. Pendiente de arranque inicial en Dockge."